{
  "name": "dynamic_sandbox",
  "version": "1.1.1",
  "private": true,
  "dependencies": {
    "@jrmst102/shared-config": "^0.1.0",
    "@jrmst102/ui-kit": "^0.1.1",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-router-dom": "^6.30.3",
    "react-scripts": "5.0.1",
    "recharts": "^3.8.0",
    "serve": "^14.2.6",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "start": "serve -s build -l ${PORT:-8080}",
    "dev": "react-scripts start",
    "build": "CI=true NODE_OPTIONS=--openssl-legacy-provider react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "yaml": "^2.8.2"
  }
}
